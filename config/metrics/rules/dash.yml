groups:
  - name: dash-host
    rules:
      - alert: DashHostHighCpu
        expr: 100 * (1 - avg by(instance) (rate(node_cpu_seconds_total{mode="idle"}[5m]))) > 85
        for: 10m
        labels: {severity: warning}
        annotations: {summary: "DASH host CPU is above 85%"}
      - alert: DashHostHighMemory
        expr: 100 * (1 - node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes) > 90
        for: 10m
        labels: {severity: warning}
        annotations: {summary: "DASH host memory is above 90%"}
      - alert: DashHostLowAvailableMemory
        expr: node_memory_MemAvailable_bytes < 2147483648
        for: 5m
        labels: {severity: critical}
        annotations: {summary: "DASH host has less than 2 GiB available"}
      - alert: DashHostFilesystemCritical
        expr: 100 * (1 - node_filesystem_avail_bytes{fstype!~"tmpfs|overlay"} / node_filesystem_size_bytes{fstype!~"tmpfs|overlay"}) > 95
        for: 5m
        labels: {severity: critical}
        annotations: {summary: "DASH host filesystem is above 95%"}
  - name: dash-datastores
    rules:
      - alert: DashPostgresDown
        expr: pg_up == 0
        for: 1m
        labels: {severity: critical}
        annotations: {summary: "DASH Postgres exporter reports down"}
      - alert: DashPostgresDeadlocks
        expr: increase(pg_stat_database_deadlocks{datname="dune_sb_1_4_0_0"}[5m]) > 0
        for: 1m
        labels: {severity: warning}
        annotations: {summary: "DASH Postgres deadlocks increased"}
      - alert: DashRabbitMQDown
        expr: rabbitmq_up == 0
        for: 1m
        labels: {severity: critical}
        annotations: {summary: "A DASH RabbitMQ broker reports down"}
      - alert: DashRabbitMQQueueBacklog
        expr: rabbitmq_queue_messages_ready > 1000
        for: 10m
        labels: {severity: warning}
        annotations: {summary: "A DASH RabbitMQ queue has a sustained backlog"}
  - name: dash-containers
    rules:
      - alert: DashContainerHighMemory
        expr: 100 * container_memory_working_set_bytes{name=~"dune_server-.*"} / container_spec_memory_limit_bytes{name=~"dune_server-.*"} > 90 and container_spec_memory_limit_bytes{name=~"dune_server-.*"} > 0
        for: 10m
        labels: {severity: warning}
        annotations: {summary: "A DASH container is above 90% of its memory limit"}
      - alert: DashContainerMemoryFailures
        expr: increase(container_memory_failcnt{name=~"dune_server-.*"}[10m]) > 0
        for: 1m
        labels: {severity: warning}
        annotations: {summary: "A DASH container memory failure counter increased"}
  - name: dash-reliability-slo
    rules:
      - alert: DashSloCollectorStale
        expr: time() - dash_slo_last_snapshot_timestamp_seconds > 180
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH reliability sampling is stale"}
      - alert: DashSloCriticalIncident
        expr: dash_slo_open_incidents{severity="critical"} > 0
        for: 1m
        labels: {severity: critical}
        annotations: {summary: "A critical DASH SLO incident is open"}
      - alert: DashSloFastBurn
        expr: dash_slo_error_budget_burn_rate{window_seconds="3600"} > 14.4 and dash_slo_observation_coverage_ratio{window_seconds="3600"} > 0.25
        for: 5m
        labels: {severity: critical}
        annotations: {summary: "A DASH objective is consuming error budget at a fast-burn rate"}
      - alert: DashSloBudgetExhausted
        expr: dash_slo_error_budget_remaining_ratio{window_seconds="2592000"} <= 0 and dash_slo_observation_coverage_ratio{window_seconds="2592000"} > 0.1
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "A DASH 30-day SLO error budget is exhausted"}
  - name: dash-capacity-intelligence
    rules:
      - alert: DashCapacityCollectorStale
        expr: time() - dash_capacity_last_sample_timestamp_seconds > 120
        for: 2m
        labels: {severity: warning}
        annotations: {summary: "DASH capacity intelligence sampling is stale"}
      - alert: DashCapacityColdStartSlow
        expr: dash_capacity_cold_start_p95_seconds > 180
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "A dynamic map cold-start p95 exceeds three minutes"}
      - alert: DashCapacityPrewarmFailed
        expr: increase(dash_capacity_prewarm_failures_total[15m]) > 0
        labels: {severity: warning}
        annotations: {summary: "A scheduled just-in-time map warm failed"}
      - alert: DashAutoscalerDemandScanFailed
        expr: dash_autoscaler_enabled == 1 and dash_autoscaler_mutations_enabled == 1 and dash_autoscaler_maintenance_paused == 0 and dash_autoscaler_demand_scan_error == 1
        for: 1m
        labels: {severity: critical}
        annotations: {summary: "DASH dynamic-map demand detection is failing"}
      - alert: DashAutoscalerDemandScanStale
        expr: dash_autoscaler_enabled == 1 and dash_autoscaler_mutations_enabled == 1 and dash_autoscaler_maintenance_paused == 0 and time() - dash_autoscaler_last_demand_scan_timestamp_seconds > 30
        for: 1m
        labels: {severity: critical}
        annotations: {summary: "DASH dynamic-map demand detection is stale"}
      - alert: DashAutoscalerReconcileFailed
        expr: dash_autoscaler_enabled == 1 and dash_autoscaler_mutations_enabled == 1 and dash_autoscaler_maintenance_paused == 0 and dash_autoscaler_reconcile_error == 1
        for: 1m
        labels: {severity: warning}
        annotations: {summary: "DASH dynamic-map lifecycle reconciliation is failing"}
      - alert: DashAutoscalerReconcileStale
        expr: dash_autoscaler_enabled == 1 and dash_autoscaler_mutations_enabled == 1 and dash_autoscaler_maintenance_paused == 0 and time() - dash_autoscaler_last_reconcile_timestamp_seconds > 120
        for: 2m
        labels: {severity: warning}
        annotations: {summary: "DASH dynamic-map lifecycle reconciliation is stale"}
      - alert: DashMaintenancePlannerCollectorDown
        expr: dash_maintenance_planner_enabled == 1 and dash_maintenance_planner_collector_up == 0
        for: 10m
        labels: {severity: warning}
        annotations: {summary: "Player-impact maintenance planning is unavailable"}
  - name: dash-desired-state
    rules:
      - alert: DashDesiredStateCollectorDown
        expr: up{job="dash-desired-state"} == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH desired-state collector is unreachable"}
      - alert: DashDesiredStateIntegrityInvalid
        expr: dash_desired_state_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH desired-state SQLite, HMAC, trigger, or event-chain verification failed"}
      - alert: DashDesiredStateUnsealed
        expr: dash_desired_state_sealed == 0
        for: 15m
        labels: {severity: critical}
        annotations: {summary: "DASH has no approved desired-state baseline"}
      - alert: DashDesiredStateCollectorStale
        expr: time() - dash_desired_state_last_observation_timestamp_seconds > 180
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH desired-state attestation is stale"}
      - alert: DashDesiredStateCriticalDrift
        expr: dash_desired_state_alertable_critical_drift > 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "Critical DASH source, configuration, or container drift is active"}
  - name: dash-change-intelligence
    rules:
      - alert: DashChangeIntelligenceCollectorDown
        expr: up{job="dash-change-intelligence"} == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH change-intelligence collector is unreachable"}
      - alert: DashChangeIntelligenceIntegrityInvalid
        expr: dash_change_intelligence_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH operational change HMAC chain or append-only enforcement failed"}
      - alert: DashChangeApprovalLedgerInvalid
        expr: dash_change_approval_enabled == 1 and dash_change_approval_ledger_valid == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH four-eyes approval request or event HMAC verification failed"}
      - alert: DashAdminAuditLedgerInvalid
        expr: dash_admin_audit_ledger_enabled == 1 and dash_admin_audit_ledger_valid == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH mutation flight-recorder HMAC chain or authenticated head is invalid"}
      - alert: DashPrivilegedRequestOutcomeUnknown
        expr: dash_admin_audit_privileged_request_oldest_open_age_seconds > 300
        for: 2m
        labels: {severity: warning}
        annotations: {summary: "A DASH privileged request was admitted but has no completion receipt"}
      - alert: DashChangeContractRefusalBurst
        expr: increase(dash_change_contract_refused_total[10m]) > 5
        for: 2m
        labels: {severity: warning}
        annotations: {summary: "DASH repeatedly refused stale, malformed, or mismatched governed change contracts"}
      - alert: DashPublicDirectoryEntryInvalid
        expr: dash_public_directory_enabled == 1 and dash_public_directory_entry_valid == 0
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "DASH public directory descriptor is invalid or missing"}
      - alert: DashPublicDirectoryEntryStale
        expr: dash_public_directory_enabled == 1 and dash_public_directory_entry_current == 0
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "DASH public directory descriptor has expired"}
      - alert: DashPlayerIdentityCollectorDown
        expr: dash_player_identity_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH cannot audit player/account identity integrity"}
      - alert: DashPlayerIdentityOrphans
        expr: dash_player_identity_orphan_rows > 0
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "DASH found player-state rows with no matching account"}
      - alert: DashPlayerIdentityDuplicates
        expr: dash_player_identity_duplicate_accounts > 0
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "DASH found accounts with multiple player-state rows"}
      - alert: DashFeatureReadinessCollectorInvalid
        expr: dash_feature_readiness_total == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH feature-readiness catalog or runtime evaluation failed"}
      - alert: DashFeatureReadinessActiveProblems
        expr: dash_feature_readiness_active_problems > 0
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "One or more active DASH features are partial, blocked, degraded, or externally blocked"}
      - alert: DashFeatureReadinessHistoryInvalid
        expr: dash_feature_readiness_history_valid == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH feature-readiness transition SQLite or HMAC chain verification failed"}
      - alert: DashFeatureReadinessRegression
        expr: increase(dash_feature_readiness_history_regressions_total[10m]) > 0
        for: 1m
        labels: {severity: warning}
        annotations: {summary: "A DASH feature regressed since the preceding readiness state vector"}
      - alert: DashCredentialLifecycleHistoryInvalid
        expr: dash_credential_lifecycle_enabled == 1 and dash_credential_lifecycle_history_valid == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH credential material-change observation SQLite or HMAC chain is invalid"}
      - alert: DashCredentialRequiredMissing
        expr: dash_credential_lifecycle_enabled == 1 and dash_credential_lifecycle_missing > 0
        for: 5m
        labels: {severity: critical}
        annotations: {summary: "One or more credentials required by active DASH features are missing"}
      - alert: DashCredentialSourcePermissionsUnsafe
        expr: dash_credential_lifecycle_enabled == 1 and dash_credential_lifecycle_insecure_permissions > 0
        for: 5m
        labels: {severity: critical}
        annotations: {summary: "One or more configured DASH credential sources have unsafe permissions"}
      - alert: DashCredentialBackupCoverageGap
        expr: dash_credential_lifecycle_enabled == 1 and dash_credential_lifecycle_backup_uncovered > 0
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "The newest DASH full backup does not cover one or more configured credentials"}
      - alert: DashCredentialRotationOverdue
        expr: dash_credential_lifecycle_enabled == 1 and dash_credential_lifecycle_rotation_overdue > 0
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "One or more scheduled or provider-managed DASH credentials are overdue for rotation"}
      - alert: DashRabbitMQRecoveryDrillConfigurationInvalid
        expr: dash_rabbitmq_restore_drill_enabled == 1 and dash_rabbitmq_restore_drill_configured == 0
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "The DASH networkless RabbitMQ recovery drill lacks a valid host workspace or Docker socket"}
      - alert: DashRabbitMQRecoveryDrillFailed
        expr: dash_rabbitmq_restore_drill_enabled == 1 and dash_rabbitmq_restore_drill_receipt_present == 1 and dash_rabbitmq_restore_drill_ok == 0
        for: 5m
        labels: {severity: critical}
        annotations: {summary: "The latest DASH networkless RabbitMQ recovery rehearsal failed or its receipt hash is invalid"}
      - alert: DashRabbitMQRecoveryDrillStale
        expr: dash_rabbitmq_restore_drill_enabled == 1 and dash_rabbitmq_restore_drill_last_finished_timestamp_seconds > 0 and time() - dash_rabbitmq_restore_drill_last_finished_timestamp_seconds > 691200
        for: 1h
        labels: {severity: warning}
        annotations: {summary: "DASH has not recorded a RabbitMQ recovery rehearsal in eight days"}
      - alert: DashIncidentHasCandidateChange
        expr: dash_change_intelligence_open_incidents_with_candidate_changes > 0
        for: 2m
        labels: {severity: warning}
        annotations: {summary: "An open DASH incident has temporally correlated preceding changes to review"}
      - alert: DashIncidentResponseDrillNotReady
        expr: dash_incident_response_last_drill_timestamp_seconds > 0 and dash_incident_response_latest_drill_ready == 0
        for: 2m
        labels: {severity: warning}
        annotations: {summary: "The latest DASH incident-response readiness drill found diagnostic or recovery-contract gaps"}
      - alert: DashIncidentResponseDrillStale
        expr: time() - dash_incident_response_last_drill_timestamp_seconds > 604800
        for: 1h
        labels: {severity: warning}
        annotations: {summary: "No successful or failed DASH incident-response readiness drill has been recorded in seven days"}
      - alert: DashIncidentReadinessNotCertified
        expr: dash_incident_readiness_certification_ready == 0
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "DASH incident-response policy has no current passing fleet-wide readiness certification"}
      - alert: DashIncidentReadinessCertificationStale
        expr: time() - dash_incident_readiness_last_certification_timestamp_seconds > 604800
        for: 1h
        labels: {severity: warning}
        annotations: {summary: "DASH fleet-wide incident-readiness certification is older than seven days"}
      - alert: DashDeploymentAssuranceCollectorInvalid
        expr: dash_deployment_assurance_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH deployment-assurance window or signed receipt verification failed"}
      - alert: DashDeploymentAssuranceNotReady
        expr: dash_deployment_assurance_latest_ready == 0
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "DASH has no passing assured deployment receipt or the latest change window failed"}
      - alert: DashDeploymentAssuranceStale
        expr: time() - dash_deployment_assurance_last_completion_timestamp_seconds > 604800
        for: 1h
        labels: {severity: warning}
        annotations: {summary: "DASH has not completed an assured deployment in seven days"}
      - alert: DashDeploymentAssuranceWindowOverdue
        expr: dash_deployment_assurance_overdue_windows > 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "A DASH assured change window expired without a completion or cancellation receipt"}
      - alert: DashUpdateReadinessCollectorInvalid
        expr: dash_update_readiness_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH game-update readiness evidence or collector verification failed"}
      - alert: DashUpdateReadinessCollectionSlow
        expr: dash_update_readiness_collection_duration_seconds > 15
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "DASH game-update readiness evidence collection exceeds 15 seconds"}
      - alert: DashUpdateReadinessPackageInspectionSlow
        expr: dash_update_readiness_package_inspection_duration_seconds > 5
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "DASH staged game package inspection exceeds five seconds"}
      - alert: DashGameUpdateCandidateBlocked
        expr: dash_update_readiness_candidate_update_required == 1 and dash_update_readiness_scheduled_ready == 0
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "A game update is available but one or more safety checks block certification"}
      - alert: DashGameUpdateCandidateUncertified
        expr: dash_update_readiness_candidate_update_required == 1 and dash_update_readiness_receipt_current == 0
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "A game update is available without a current candidate-bound readiness receipt"}
      - alert: DashMaintenanceOutcomeCollectorInvalid
        expr: dash_maintenance_outcome_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH maintenance outcome evidence failed cryptographic or semantic verification"}
      - alert: DashMaintenanceOutcomeFailed
        expr: dash_maintenance_outcome_retained_receipts > 0 and dash_maintenance_outcome_latest_ready == 0
        for: 2m
        labels: {severity: warning}
        annotations: {summary: "The latest DASH restart or shutdown maintenance contract failed"}
      - alert: DashCommunityCanaryCollectorInvalid
        expr: dash_community_canary_enabled == 1 and dash_community_canary_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH Community Rewards canary evidence failed cryptographic or semantic verification"}
      - alert: DashCommunityCanaryNotCurrent
        expr: dash_community_canary_enabled == 1 and dash_community_canary_current_ready == 0
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "The active Community Rewards policy lacks a current passing isolated transaction canary"}
      - alert: DashCreatorCanaryCollectorInvalid
        expr: dash_creator_canary_enabled == 1 and dash_creator_canary_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH Creator/Modding canary evidence failed cryptographic or semantic verification"}
      - alert: DashCreatorCanaryNotCurrent
        expr: dash_creator_canary_enabled == 1 and dash_creator_canary_current_ready == 0
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "The active Creator/Modding inputs lack a current passing isolated lifecycle canary"}
      - alert: DashPublicIpCanaryCollectorInvalid
        expr: dash_public_ip_canary_enabled == 1 and dash_public_ip_canary_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH public-IP repair canary evidence failed cryptographic or semantic verification"}
      - alert: DashPublicIpCanaryNotCurrent
        expr: dash_public_ip_canary_enabled == 1 and dash_public_ip_canary_current_ready == 0
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "The active public-IP repair inputs lack a current passing isolated lifecycle canary"}
      - alert: DashPublicIpMonitorNotArmed
        expr: dash_public_ip_canary_enabled == 1 and dash_public_ip_monitor_armed == 0
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "The hostname-gated DASH public-IP monitor remains in dry-run mode"}
      - alert: DashCanaryAutopilotCollectorInvalid
        expr: dash_canary_autopilot_enabled == 1 and dash_canary_autopilot_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH isolated-proof autopilot state or a target evidence collector is invalid"}
      - alert: DashCanaryAutopilotWorkerStopped
        expr: dash_canary_autopilot_enabled == 1 and dash_canary_autopilot_worker_running == 0
        for: 5m
        labels: {severity: critical}
        annotations: {summary: "DASH isolated-proof autopilot is enabled but its worker is not running"}
      - alert: DashCanaryAutopilotRefreshOverdue
        expr: dash_canary_autopilot_enabled == 1 and dash_canary_autopilot_due > 0
        for: 30m
        labels: {severity: warning}
        annotations: {summary: "One or more isolated DASH proofs remain due after the automatic refresh window"}
      - alert: DashOperationsBriefingCollectorInvalid
        expr: dash_operations_briefing_enabled == 1 and dash_operations_briefing_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH operator-briefing evidence failed cryptographic, semantic, or source collection verification"}
      - alert: DashOperationsBriefingWorkerStopped
        expr: dash_operations_briefing_enabled == 1 and dash_operations_briefing_worker_running == 0
        for: 5m
        labels: {severity: critical}
        annotations: {summary: "DASH operator briefing is enabled but its change-aware worker is not running"}
      - alert: DashOperationsBriefingNotCurrent
        expr: dash_operations_briefing_enabled == 1 and dash_operations_briefing_current == 0
        for: 30m
        labels: {severity: warning}
        annotations: {summary: "The latest signed DASH operator briefing is stale or predates the current source state"}
      - alert: DashOperationsBriefingRefreshPending
        expr: dash_operations_briefing_enabled == 1 and dash_operations_briefing_refresh_pending == 1
        for: 2m
        labels: {severity: warning}
        annotations: {summary: "An event-driven DASH operator briefing refresh remained pending beyond its bounded change interval"}
      - alert: DashOperationsBriefingCriticalActions
        expr: dash_operations_briefing_enabled == 1 and dash_operations_briefing_critical > 0
        for: 5m
        labels: {severity: critical}
        annotations: {summary: "The current signed DASH operator briefing contains one or more critical actions"}
      - alert: DashBackupScheduleCollectorInvalid
        expr: dash_backup_schedule_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH cannot read the automatic full-backup scheduler state"}
      - alert: DashBackupScheduleWorkerStopped
        expr: dash_backup_schedule_enabled == 1 and dash_backup_schedule_worker_running == 0
        for: 5m
        labels: {severity: critical}
        annotations: {summary: "Automatic full backups are enabled but the DASH scheduler worker is not running"}
      - alert: DashBackupScheduleRunFailed
        expr: dash_backup_schedule_enabled == 1 and dash_backup_schedule_last_run_timestamp_seconds > 0 and dash_backup_schedule_last_run_ok == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "The latest automatic full backup failed coverage or verification"}
      - alert: DashBackupScheduleOverdue
        expr: dash_backup_schedule_enabled == 1 and dash_backup_schedule_overdue_seconds > 900
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "An automatic full backup remains overdue beyond its lock/failure retry window"}
      - alert: DashOperationsCalendarCollectorInvalid
        expr: dash_operations_calendar_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH cannot build the unified operations calendar"}
      - alert: DashOperationsCalendarCriticalConflict
        expr: dash_operations_calendar_critical_conflicts > 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "A disruptive operation overlaps recovery work or another disruptive operation"}
      - alert: DashOperationsCalendarWarningConflict
        expr: dash_operations_calendar_warning_conflicts > 0
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "The operations calendar contains overlapping recovery or preparation work"}
      - alert: DashOperationsCalendarMaintenanceUncovered
        expr: dash_operations_calendar_uncovered_disruptive_windows > 0
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "Executing maintenance is not fully covered by a planned SLO maintenance exclusion"}
      - alert: DashAlertInboxCollectorInvalid
        expr: dash_alert_inbox_enabled == 1 and dash_alert_inbox_collector_up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "DASH cannot authoritatively poll or persist Prometheus alert state"}
      - alert: DashAlertInboxWorkerStopped
        expr: dash_alert_inbox_enabled == 1 and dash_alert_inbox_worker_running == 0
        for: 5m
        labels: {severity: critical}
        annotations: {summary: "The DASH on-call alert inbox is enabled but its polling worker is not running"}
      - alert: DashPeerWatchCollectorInvalid
        expr: dash_peer_watch_enabled == 1 and dash_peer_watch_collector_up == 0
        for: 15m
        labels: {severity: warning}
        annotations: {summary: "DASH cannot currently verify the pinned ecosystem peer catalogue"}
      - alert: DashPeerRevisionDrift
        expr: dash_peer_watch_drifted > 0
        for: 5m
        labels: {severity: warning}
        annotations: {summary: "One or more pinned Dune hosting peers have advanced and require a feature review"}
      - alert: DashPeerWatchSourceErrors
        expr: dash_peer_watch_errors > 0
        for: 30m
        labels: {severity: warning}
        annotations: {summary: "One or more pinned peer repositories cannot be observed"}
